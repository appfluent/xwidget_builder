# XWidget Config

icons:
  target: "src/generated/src/icons_test.g.dart"
  sources: [
    "src/icons_spec.dart"
  ]

inflaters:
  target: "src/generated/src/inflaters_test.g.dart"
  imports: [ ]
  sources: [
    "src/inflater_spec.dart"
  ]
  includes: [
    "src/test_includes.dart"
  ]
  constructor_exclusions: [
    "TestExclusions:excludedArg"
  ]
  constructor_arg_defaults:
    "TestExclusions:visible": "'fallbackValue'"
  constructor_arg_parsers:
    "TestExclusions:visible": "value.trim()"

controllers:
  target: "src/generated/src/controllers_test.g.dart"
  sources: [
    "src/test_controllers.dart"
  ]

schema:
  attribute_exclusions: [
    "TestExclusions:excludedAttr"
  ]
