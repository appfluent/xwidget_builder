import 'dart:core';

// Include fixture: every line except imports is copied verbatim into the
// generated inflaters file by InflaterBuilder.

String testIncludeHelper(String input) => input.toUpperCase();
